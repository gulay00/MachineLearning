Book Recommendation System

A simple book recommendation system built with Python.

Features

User inputs a keyword (e.g., "Clara")

Returns all books in the dataset that contain the keyword in their title

Books are sorted by Book-Rating

Returns an empty list if no matching books are found
